#include "biblioteca.h"
#include <stdio.h>
#include <stdlib.h>
#include <math.h>

void vetor(int* p, int tam, &maior, &menor){
   
    int i = 0;
    int num;

    tMaximos numMaximos;

    printf("Informe uma cadeia de numeros:\n");
    scanf("%i",&i);
    
    int nvet[i];

    for(int j=0, j<i; j++){
        scanf("%i", &num);
        nvet[j] = num;
    } 
}