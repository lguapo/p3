




//copilação
/*
    gcc -c arqfonte.c
    gcc -c main.c
    gcc -o main.exe main.o arqfonte.o
    gcc -c -g* .c
   gcc*.o -o programa
   .\programa.exe

*/
-----------------------------
.h
----------------------------
#ifndef _BIBLIOTECA_H
#define _BIBLIOTECA_H
/
/
/
//só prototipar funções
//float notas(tAlunos);
/
/
#endif
------------------------
arq fonte:(.c)

#include "biblioteca.h"
#include <std--.h>

//funções desenvolvidas
----------------------------
main.c
/*
#include <stdio.h>
#include "biblioteca.h"
#include <math.h>

int main(void){
  tInvestimento a;
  float resultado;
  a.mInvestido = 2500;
  a.tJuros = 10;
  a.tempoInvestido = 2;

  resultado = mInvestido(a);
  printf("%2.f\n", resultado);

  resultado = tJuros(a);
  printf("%2.f\n", resultado);

  resultado = tempoInvestido(a);
  printf("%2.f\n", resultado);
  
  resultado = mInvestidoJS(a);
  printf("%2.f\n", resultado);

  resultado = mInvestidoJC(a);
  printf("%2.f\n", resultado);
}
*/
//chamar as funções
